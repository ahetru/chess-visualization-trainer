# chess-visualization-trainer

## Local development

- Backend uses Maven Wrapper (`backend/mvnw`) so no global Maven installation is required.
- Frontend uses npm scripts from `frontend/`.

## Test commands

- Full test suite:
  - `make test`
- Backend only:
  - `make backend-test`
- Frontend only:
  - `make frontend-test`