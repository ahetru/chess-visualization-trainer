# Base URL of the InnerChess backend API, read by Vite at dev-server start
# and baked into the frontend bundle at build time.
# - Local dev (make dev / make backend-run): http://localhost:8080
# - Docker compose stack: injected by docker-compose.yml, this file is not used
VITE_API_URL=http://localhost:8080
